import { describe, expect, it } from "bun:test";

describe("greet", () => {
  it("returns a greeting string", () => {
    // Replace with your own import and assertions
    expect("Hello, world!").toBe("Hello, world!");
  });
});
